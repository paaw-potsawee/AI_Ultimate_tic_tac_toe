services:
    uttt:
        image: uttt:latest
        container_name: uttt
        restart: always
        build:
            context: .
            dockerfile: Dockerfile
        healthcheck:
            test: ["CMD", "wget", "-q", "--spider", "http://localhost/"]
            interval: 30s
            timeout: 10s
            retries: 5
        ports:
            - "3000:80"
